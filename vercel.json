{
  "framework": "nextjs",
  "buildCommand": "npx prisma db push --accept-data-loss && npx prisma generate && npx next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "regions": ["sin1"]
}
